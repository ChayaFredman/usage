<%= form_for(@rx_by_day) do |f| %>
  <% if @rx_by_day.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rx_by_day.errors.count, "error") %> prohibited this rx_by_day from being saved:</h2>

      <ul>
      <% @rx_by_day.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
